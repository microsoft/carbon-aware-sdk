apiVersion: v1
kind: Service
metadata:
  name: {{ include "ca-deploy-charts.fullname" . }}
  labels:
    {{- include "ca-deploy-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ca-deploy-charts.selectorLabels" . | nindent 4 }}
