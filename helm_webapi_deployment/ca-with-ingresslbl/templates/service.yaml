apiVersion: v1
kind: Service
metadata:
  name: {{ include "ca-with-ingresslbl.fullname" . }}
  labels:
    {{- include "ca-with-ingresslbl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ca-with-ingresslbl.selectorLabels" . | nindent 4 }}
