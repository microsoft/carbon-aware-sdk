apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ca-with-ingresslbl.fullname" . }}-test-connection"
  labels:
    {{- include "ca-with-ingresslbl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ca-with-ingresslbl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
